env-dir:
  stg: ${HOME}/_git/_stg
  prd: ${HOME}/_git/_prd

app-bin:
  amnezic: ${ENV_DIR}/amnezic-go/bin/amnezic-go
  games: ${ENV_DIR}/games-go/bin/games-go

app-run:
  amnezic: ${ENV_DIR}/amnezic-go/scripts/run.sh
  games: ${ENV_DIR}/games-go/scripts/run.sh