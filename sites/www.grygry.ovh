# https
server {
        server_name             grygry.ovh www.grygry.ovh;

        listen                  443 ssl;
        include                 /var/nginx/.conf/ssl.conf;
        access_log              /var/log/nginx/www.log;
        error_log               /var/log/nginx/www.log;
        root                    /var/nginx/www;
        location / {
                index index.html;
                try_files $uri $uri/ $uri.html =404;
        }
}

# enforce https
server {
        server_name             _;
        listen                  80 default_server;
        listen                  [::]:80 default_server;
        access_log              /var/log/nginx/http.log;
        error_log               /var/log/nginx/http.log;
        return                  301 https://$host$request_uri;
}

# reject others
server {
        server_name             _;
        listen                  443 default_server ssl;
        include                 /var/nginx/.conf/ssl.conf;
        access_log              /var/log/nginx/others.log;
        error_log               /var/log/nginx/others.log;
        return                  444;
}
