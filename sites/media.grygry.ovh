# http
server {
        listen          80;
        listen          [::]:80;

        access_log      /var/log/nginx/media.access.log;
        root            /home/ubuntu/www/media;

        location / {
                deny all;
                location ~ "\.(mp3)$" {
                        allow all;
                }
        }
}