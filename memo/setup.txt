
#
# .bashrc
#

# add lines to ~/.bashrc

if [ -f ~/.bashrc.user ]; then
    . ~/.bashrc.user
fi

#
# oci-tools
#

mkdir _git
cd _git
git clone git@github.com:gvaligiani/bash-tools bash-tools
git clone git@github.com:gre-ory/oci-tools oci-tools

#
# .bashrc
#

ln -s ~/_git/oci-tools/bash/.bashrc.user
source ~/.bashrc

#
# git
#

ln -s ~/_git/oci-tools/bash/.gitconfig

#
# nginx
#

mkdir _www
mkdir _www/amnezic
mkdir _www/media
mkdir _www/static

chmod 777 _www _www/amnezic _www/media _www/static

ln -s /home/ubuntu/_git/oci-tools/html _www/www
ln -s /home/ubuntu/_git/stg/amnezic-react/build-stg _www/amnezic/stg
ln -s /home/ubuntu/_git/prd/amnezic-react/build-prd _www/amnezic/prd

sudo ln -s /home/ubuntu/_www/amnezic /var/www/amnezic
sudo ln -s /home/ubuntu/_www/media /var/www/media
sudo ln -s /home/ubuntu/_www/static /var/www/static
sudo ln -s /home/ubuntu/_www/www /var/www/www